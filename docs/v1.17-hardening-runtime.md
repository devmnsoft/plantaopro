# v1.17 Hardening Runtime

Status: funcional pendente QA navegada. Esta versão não declara produção.

- Swagger: contrato reforçado para manter `GET /api/dashboard` único e rotas v1.12/v1.13 versionadas.
- Logging: status final passa a ser responsabilidade do `RequestLoggingMiddleware` após `await next()`.
- Claims: Dashboard e Mobile Home retornam `401` amigável quando `uid` está ausente ou inválido.
- DevelopmentSeed: execução controlada por `DevelopmentSeed:Enabled`.
