# v1.17 Smoke Bloqueante

Status: funcional pendente execução em ambiente com API e banco.

`scripts/smoke-test-v116.sh` e `scripts/smoke-test-v117.sh` falham para health/swagger indisponíveis, HTTP `000`, `404`, `500` e ausência de token em endpoints autenticados. Os relatórios JSON registram `PASSED` ou `FAILED` por rota e não imprimem senha ou token.
