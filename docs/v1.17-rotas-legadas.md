# v1.17 Rotas Legadas

Status: compatibilidade documentada.

As rotas operacionais legadas não versionadas de v1.13 foram removidas do contrato público; o contrato preservado é `/api/v113/*`. O dashboard legado permanece em `/api/v112/dashboard` e `/api/v113/dashboard`, sem conflito com `/api/dashboard`.
