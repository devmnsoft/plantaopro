# Smoke visual v1.84.0

**Status: BLOQUEADO neste ambiente.** A aplicação .NET não pôde ser iniciada porque `dotnet` não está instalado; portanto não há screenshots ou resultados runtime inventados.

O runner grava em `artifacts/ui-audit/screenshots/v184/` e `v184-visual-smoke-results.json`. Foram adicionados os checks `businessActionsEndpointBacked`, `disabledActionsReduced`, `criticalActionsHaveReasonModal`, `mutationButtonsHaveLoading`, `businessErrorsHandled` e `noFakeSuccess`, preservando rotas e checks anteriores.
