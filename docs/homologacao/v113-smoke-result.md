# Resultado smoke v1.13

Arquivo inicial versionado para evidência. A execução real do smoke sobrescreve este relatório e o JSON correspondente.

- Base URL padrão: `http://localhost:5000`
- Token: opcional via variável de ambiente, sem impressão em log
- Status: pendente de execução em ambiente runtime com API e PostgreSQL
