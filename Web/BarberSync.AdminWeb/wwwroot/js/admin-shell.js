document.addEventListener('click',async(e)=>{const btn=e.target.closest('[data-action]');if(!btn)return;const action=btn.dataset.action;if(action==='create')return BarberSyncCrud.create(btn.dataset.entity||'clients');if(action==='refresh')return BarberSyncDashboard.render();if(action==='copilot'){const res=await BarberSyncApi.post('/copilot/ask',{question:'Como aumentar receita hoje?'});BarberSyncEventBus.publish('copilot:ask',res.data);return BarberSyncToast.show(res.data?.answer||'Copilot respondeu.');}BarberSyncEventBus.publish(`ui:${action}`,{id:btn.dataset.id});BarberSyncToast.show(`${action} aplicado em ${btn.dataset.id||'item demo'}`);});
