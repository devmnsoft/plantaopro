@model IEnumerable<PlantaoPro.Web.Models.QuickActionViewModel>
<section class="quick-actions-grid pp-module-grid" aria-label="Ações rápidas">
@foreach (var action in Model)
{
    <partial name="_ProfileHomeCard" model="action" />
}
</section>
