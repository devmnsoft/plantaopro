# Smoke visual v1.76.0 — preparação e bloqueio

**NÃO EXECUTADO contra runtime real.** Não há SDK `dotnet`; portanto não foram criados screenshots nem JSON de resultados. O runner aponta para `screenshots/v176/`, cobre 23 rotas, oito viewports e os contratos clínico, operacional e de ações sem backend.

Execute com `PLANTAOPRO_BASE_URL`, `PLANTAOPRO_STORAGE_STATE` e `scripts/ui/run-visual-smoke.sh`; para rotas públicas, use `PLANTAOPRO_PUBLIC_ONLY=1`.
