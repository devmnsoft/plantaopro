# Smoke visual v1.71.0

Status runtime: **não executado nesta geração do artefato**. Não há declaração de aprovação sem aplicação ativa, APIs acessíveis, autenticação válida e navegador Playwright.

O runner grava `v171-visual-smoke-results.json`, atualiza este resumo e cria imagens em `screenshots/v171/`. O contrato contém 22 rotas, oito viewports, overflow, cards, tabelas, dialogs, labels, shell, Command Palette, Notification Drawer e estado honesto de Minha Assinatura.
