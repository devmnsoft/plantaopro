# v1.12 build/test report

Data: 2026-07-10.

## Execução local

- `dotnet clean && dotnet restore && dotnet build && dotnet test`: não executado com sucesso porque o container local retornou `/bin/bash: line 1: dotnet: command not found`.
- Correção aplicada: a entrega inclui testes contratuais e scripts de smoke para execução em ambiente com SDK .NET/CI.

## Banco

Os scripts `scripts/db-debug-scriptcompleto.ps1` e `.cmd` não existem neste repositório PlantaoPro local; a validação de PostgreSQL real deve ser executada no pipeline que provisiona banco limpo.
- `cd mobile/PlantaoPro.App && npm install && npm run typecheck`: `npm install` concluiu, mas `npm run typecheck` falhou porque o `package.json` atual não define o script `typecheck`.
