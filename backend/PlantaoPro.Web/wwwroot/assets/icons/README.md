# Iconografia first-party

Use exclusivamente `<app-icon name="Calendar" size="20" />`. O Tag Helper valida `AppIconKey`, resolve o `symbol` local e usa `Unknown` como fallback. Ícones ao lado de texto são decorativos; ícones informativos recebem `label`.
