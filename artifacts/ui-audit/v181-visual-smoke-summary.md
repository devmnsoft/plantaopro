# Smoke visual v1.81.0

Contrato atualizado para 24 rotas, oito viewports e checks administrativos v1.81. A execução real exige `PLANTAOPRO_BASE_URL` e uma sessão `PLANTAOPRO_STORAGE_STATE`; resultados e screenshots não são fabricados. Consulte `scripts/ui/visual-smoke.mjs` e os runners `.sh`/`.ps1`.

**Status:** pendente de runtime autenticado.
