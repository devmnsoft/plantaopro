export class WorkItemDrawer{
 constructor(api,toast){this.api=api;this.toast=toast;this.drawer=document.querySelector('[data-work-item-drawer]');}
 bind(){document.querySelectorAll('[data-work-item-open]').forEach(b=>b.addEventListener('click',()=>this.open(b.dataset.workItemOpen,b)));this.drawer.querySelector('[data-comment-form]').addEventListener('submit',e=>this.comment(e));}
 async open(id,trigger){this.id=id;this.trigger=trigger;this.drawer.hidden=false;document.querySelector('[data-overlay-backdrop]').hidden=false;const content=this.drawer.querySelector('[data-detail-content]');content.innerHTML='<div class="skeleton-list"></div>';const response=await fetch(`${this.api}/work-items/${id}`);this.item=await response.json();this.drawer.querySelector('[data-detail-title]').textContent=this.item.titulo;content.innerHTML=`<p>${this.item.descricao}</p><dl><dt>Status</dt><dd>${this.item.status}</dd><dt>Prioridade</dt><dd>${this.item.prioridade}</dd><dt>Versão</dt><dd>${this.item.versao}</dd></dl>`;this.drawer.querySelector('button').focus();}
 async comment(event){event.preventDefault();const button=event.submitter;const text=event.target.querySelector('textarea');button.disabled=true;button.textContent=button.dataset.busyText;const response=await fetch(`${this.api}/work-items/${this.id}/comentar`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({comment:text.value,version:this.item.versao,idempotencyKey:crypto.randomUUID()})});button.disabled=false;button.textContent='Salvar comentário';if(response.ok){this.item=await response.json();text.value='';this.toast('Comentário salvo','A atualização já está disponível no histórico da tarefa.');}else this.toast('Comentário não salvo','Revise o texto ou atualize a tarefa antes de tentar novamente.','error');}
}
