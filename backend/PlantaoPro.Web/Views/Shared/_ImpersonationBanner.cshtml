@if (User.FindFirst("impersonation")?.Value == "true") { <div class="alert alert-warning d-flex gap-2" role="status"><i class="bi bi-shield-exclamation"></i><div><strong>Acesso assistido ativo.</strong> Operações sensíveis estão bloqueadas durante a impersonação.</div></div> }
