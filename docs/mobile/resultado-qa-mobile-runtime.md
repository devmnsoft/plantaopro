# Resultado QA mobile runtime

Data: 2026-07-09.

## Comandos previstos

```bash
cd mobile/PlantaoPro.App
npm install
npm run start -- --help
```

## Resultado

Pendente de execução runtime/interativa. A revisão estática confirma preparação para `EXPO_PUBLIC_API_BASE_URL`, sem `Alert.alert` no código-fonte `src`, e sem log de token identificado nos contratos existentes.

## Fluxos pendentes de runtime

Login, home, plantões disponíveis, detalhe, solicitação, convites, aceite/recusa com motivo, escalas, pagamentos, notificações, disponibilidade, preferências e perfil.

## Status final

**Funcional pendente QA / Bloqueado por ambiente interativo Expo**. Não declarar produção.
