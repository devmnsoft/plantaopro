# Formulários Funcionais

POSTs usam antiforgery, serviço Web e endpoints API reais definidos em `ApiEndpoint`. Em erro, o modelo retorna à view preservando dados. O botão salvar usa loading para evitar duplo envio.
