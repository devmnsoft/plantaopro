@using System.Text.Json
@{ ViewData["Title"] = "Performance"; var jsonOptions = new JsonSerializerOptions { WriteIndented = true }; }
<partial name="_PageHeader" model='new PlantaoPro.Web.Models.PageHeaderViewModel("Performance","Endpoints mais lentos e tempo médio de resposta.","bi-speedometer2","Voltar",Url.Action("Index", "Observabilidade"),null,false)' />
<div class="card border-0 shadow-sm"><div class="card-body"><pre class="small mb-0 bg-light border rounded p-3">@JsonSerializer.Serialize(ViewBag.Performance, jsonOptions)</pre></div></div>
