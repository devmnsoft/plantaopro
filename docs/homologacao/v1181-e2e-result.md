# Homologação E2E v1.18.1

Status local: pendente de execução no GitHub Actions porque o container de desenvolvimento não possui SDK .NET nem PostgreSQL ativo.

O workflow passa a executar build/test, instalação limpa, upgrade real, runtime E2E, mobile check real e verificação de segurança do repositório.
