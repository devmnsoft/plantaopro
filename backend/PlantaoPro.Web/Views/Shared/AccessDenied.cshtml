@{ ViewData["Title"] = "Acesso negado"; }
<div class="container py-5">
    <div class="card border-0 shadow-sm mx-auto" style="max-width: 720px;">
        <div class="card-body text-center p-5">
            <i class="bi bi-shield-lock text-warning display-4"></i>
            <h1 class="h3 mt-3">Acesso negado</h1>
            <p class="text-muted mb-4">Seu perfil não possui permissão para acessar este recurso ou os dados pertencem a outro cliente.</p>
            <a class="btn btn-primary" asp-controller="Home" asp-action="Dashboard">Voltar ao dashboard</a>
        </div>
    </div>
</div>
