# Homologação visual v1.70.1

O roteiro automatizado cobre as rotas e os oito viewports definidos em `scripts/ui/visual-smoke.mjs`. A execução real requer aplicação ativa e estado Playwright autenticado; nenhum screenshot foi fabricado nesta revisão.

Execute `PLANTAOPRO_BASE_URL=http://localhost:5000 PLANTAOPRO_STORAGE_STATE=artifacts/ui-audit/auth/storage-state.json bash scripts/ui/run-visual-smoke.sh`.
